/**
 * 콘솔에 로그를 출력합니다.
 *
 * `console`의 직접 사용은 허용하지 않습니다.
 *
 * @example
 * ```ts
 * customConsole.log('log')
 * customConsole.info('info')
 * customConsole.debug('debug')
 * customConsole.warn('warn')
 * customConsole.error('error')
 * ```
 */
export const customConsole = console;
