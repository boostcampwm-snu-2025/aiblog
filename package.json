{
    "devDependencies": {
        "husky": "^9.1.7",
        "lint-staged": "^16.2.6",
        "prettier": "^3.6.2"
    },
    "scripts": {
        "prepare": "husky"
    },
    "lint-staged": {
        "client/src/**/*.{ts,tsx}": [
            "npm run lint --prefix client -- --fix",
            "bash -c 'npm run typecheck --prefix client'"
        ]
    }
}
